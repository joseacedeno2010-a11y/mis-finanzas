/* Configuración de Firebase para la sincronización en la nube.
   Se rellena con los datos del proyecto en console.firebase.google.com (Configuración del proyecto → Tus apps → SDK). */
window.FIREBASE_CONFIG = null;
